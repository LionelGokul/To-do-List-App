import Logo from "./logo";

const Header = () => {
  return (
    <header className="flex justify-center items-center min-h-[200px] py-10 bg-black">
      <Logo />
    </header>
  );
};

export default Header;
